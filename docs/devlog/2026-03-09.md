# 2026-03-09 Devlog

## 01:10 EST - Shared Album Uploader Attempt Stopped (Codex)

### Summary
Attempted to validate and harden the Swift uploader rewrite for shared iCloud album photos. Build and CLI refactor work succeeded, but reliable automated export from Photos shared assets remains unresolved in production flow. Work is being stopped here by request.

### What Was Done
- Rebuilt and validated Swift CLI locally (`swift build` passes).
- Implemented run workspace artifacts:
  - `run.json`
  - `manifest.jsonl`
  - `failures.jsonl`
- Added retry/status plumbing:
  - `--status --resume <run-id>`
  - `--retry --resume <run-id>`
  - failed-asset filtering from prior manifest
- Added upload retry/backoff and classified upload errors.
- Added per-item manifest fields (`filePath`, `checksum`) and persistence.
- Implemented Photos UI automation export-first path with fallback handling.

### Live Validation Findings
- Photos selection detection works (`count of selection = 5`).
- Authentication and upload session initialization work.
- Shared-album export still fails to produce files via automation path.
- Direct AppleScript export fallback remains the known hang path for this dataset.
- A manual-assisted fallback prompt was added to avoid indefinite hanging when automation yields zero files.

### Outcome
- Core reliability/refactor scaffolding is now in place.
- Shared album export remains the blocker for fully unattended operation.
- Per user request, implementation work is stopped at this point.
