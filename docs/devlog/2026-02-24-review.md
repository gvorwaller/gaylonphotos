# Hostile Review Loop — Summary for CC2

**Reviewer:** Claude Opus 4.6 (hostile adversarial review, 5 rounds)
**Date:** 2026-02-24
**Scope:** Full repo (~57 files, ~5700 lines) — all new code

## CC2: Please commit all changes with the message below

All 12 `td` items in review have been approved. The working tree has all fixes applied and the build passes clean. Please commit with:

```
Hostile review loop: 5 rounds, 46 issues fixed

Security: slug validation on all API routes, magic bytes on upload,
EXIF field picking, mass assignment prevention, error sanitization.
Correctness: $derived.by, TOCTOU races, state re-sync, photo counts.
Robustness: collision-safe IDs, bare catch elimination, parallel loading.
```

---

## Scoreboard

| Round | P1 | P2 | P3 | Notes |
|-------|----|----|-----|-------|
| 1 | 5 | 7 | 7 | Initial sweep |
| 2 | 8 | 14 | 11 | Deep dive |
| 3 | 1 | 3 | 5 | Targeted |
| 4 | 1 | 4 | 4 | Regression check |
| 5 | 0 | 4 | 5 | Final — 0 P1s |

**Result: 0 P1, 2 P2 accepted-as-design, 3 P3 accepted-as-minor**

---

## Files Modified (for commit staging)

### Server Modules
- `src/lib/server/json-store.js` — async updaterFn guard
- `src/lib/server/auth.js` — SESSION_TTL_MS rename for clarity
- `src/lib/server/photos.js` — writeJson import fix, validateSlug, processAndUpload rewrite (no placeholder leak), derivePhotoId collision loop, deletePhoto TOCTOU fix, updatePhotoGps dedup
- `src/lib/server/collections.js` — createCollection TOCTOU fix + fresh-install bootstrap, deleteCollection TOCTOU fix + reordered operations
- `src/lib/server/itinerary.js` — validateSlug, addStop bare-catch elimination, data.stops guard

### API Routes
- `src/routes/api/photos/+server.js` — slug validation on all methods, magic bytes check, error sanitization
- `src/routes/api/collections/+server.js` — slug validation on PUT/DELETE, mass assignment fix
- `src/routes/api/geotag/+server.js` — slug validation
- `src/routes/api/itinerary/+server.js` — slug validation on all methods

### Hooks
- `src/hooks.server.js` — admin route prefix match fix

### Page Routes
- `src/routes/+page.server.js` — separate photoCounts, parallel loading
- `src/routes/+page.svelte` — pass photoCounts prop
- `src/routes/admin/+page.server.js` — parallel photo loading
- `src/routes/admin/[collection]/+page.svelte` — photos re-sync on navigation

### Components
- `src/lib/components/Landing.svelte` — photoCounts prop, correct count display
- `src/lib/components/common/Map.svelte` — interval timeout, onerror handler, strokeColor literal
- `src/lib/components/common/PhotoDetail.svelte` — tags?.length guard
- `src/lib/components/common/Lightbox.svelte` — navIndex reset on prop change
- `src/lib/components/travel/Timeline.svelte` — collectionSlug prop, $derived.by
- `src/lib/components/travel/ItineraryMap.svelte` — $derived.by
- `src/lib/components/wildlife/SpeciesGrid.svelte` — $derived.by
- `src/lib/components/action/SpotGallery.svelte` — $derived.by
- `src/lib/components/admin/CollectionEditor.svelte` — deleteSlug capture fix
- `src/lib/components/admin/PhotoEditor.svelte` — state re-sync on prop change
- `src/lib/components/admin/GeoTagger.svelte` — re-sync on prop change, $derived.by
- `src/lib/components/admin/AdminNav.svelte` — await goto on logout

### Client Modules
- `src/lib/api.js` — Content-Type check before res.json()
- `src/lib/stores.js` — dead isAdmin store removed

### Scripts
- `scripts/ingest-photos.js` — derivePhotoId fixes, explicit metadata picking, slug validation

---

## Accepted Items (not bugs, conscious decisions)

1. **Deprecated `google.maps.Marker`** — Works today. Migration to AdvancedMarkerElement requires Map ID config, defer to when Google removes legacy API.
2. **Orphaned Spaces objects on JSON write failure** — Extremely rare (disk full). Re-upload works. Cleanup script can be added later if needed.
3. **`updateItinerary` last-writer-wins** — Inherent to PUT-replace-all design. Would need optimistic concurrency (version field) to fix. Over-engineering for single-admin.
4. **Session map unbounded growth** — Single admin, logins are infrequent. Would need periodic GC sweep to fix. Not worth the complexity.
5. **`reorderStops` is dead code** — Exported but unused. Intentionally kept for future itinerary drag-and-drop feature per DESIGN_SPEC.
