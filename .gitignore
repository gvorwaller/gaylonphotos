# Dependencies
node_modules/

# SvelteKit
.svelte-kit/
build/

# Runtime data (JSON "database" — managed via admin UI, not git)
data/
data-prod/

# Test data
test-data/

# Source photos (large binaries — don't commit)
/photos/

# Environment
.env
.env.local
.env.production

# OS
.DS_Store
Thumbs.db

# IDE / tooling
.vscode/
.idea/
.todos/
.sidecar/
.sidecar-agent
.sidecar-task
.sidecar-pr
.sidecar-start.sh
.sidecar-base
.td-root

# Swift CLI build artifacts
mac/photo-uploader-cli/.build/
mac/photo-uploader-cli/.swiftpm/
.photo-uploader-runs/
